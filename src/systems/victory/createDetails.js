import * as THREE from 'three';export function createDetails({materials,quality='medium'}){const group=new THREE.Group();group.name='VictoryDetails';const anchor=new THREE.Group(),shaft=new THREE.Mesh(new THREE.CylinderGeometry(.11,.11,3.2,10),materials.cannon),arm=new THREE.Mesh(new THREE.CylinderGeometry(.09,.09,2.1,10),materials.cannon);shaft.rotation.z=Math.PI/2;anchor.add(shaft,arm);anchor.position.set(-6.4,.7,-17.5);anchor.rotation.z=.3;group.add(anchor);const boat=new THREE.Mesh(new THREE.CapsuleGeometry(1,4.2,5,quality==='low'?8:14),materials.deck);boat.scale.set(1,.42,1);boat.rotation.z=Math.PI/2;boat.position.set(0,7.25,4.8);group.add(boat);const wheel=new THREE.Group(),rim=new THREE.Mesh(new THREE.TorusGeometry(.8,.08,8,22),materials.darkWood);rim.rotation.y=Math.PI/2;wheel.add(rim);for(let i=0;i<8;i++){const spoke=new THREE.Mesh(new THREE.CylinderGeometry(.035,.035,1.75,6),materials.darkWood);spoke.rotation.z=i*Math.PI/4;wheel.add(spoke)}wheel.position.set(0,8.35,14.4);group.add(wheel);return group;}
