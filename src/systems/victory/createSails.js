import * as THREE from 'three';const curvedSail=(width,height,materials,quality)=>{const cols=quality==='low'?8:quality==='medium'?12:16,rows=quality==='low'?6:quality==='medium'?9:12,positions=[],indices=[];for(let y=0;y<=rows;y++){const v=y/rows;for(let x=0;x<=cols;x++){const u=x/cols;positions.push((u-.5)*width*(.94-v*.12),-v*height,Math.sin(u*Math.PI)*Math.sin(v*Math.PI)*width*.075)}}for(let y=0;y<rows;y++)for(let x=0;x<cols;x++){const a=y*(cols+1)+x,b=a+cols+1,c=a+cols+2,d=a+cols+1;indices.push(a,d,c,a,c,b)}const geometry=new THREE.BufferGeometry();geometry.setAttribute('position',new THREE.Float32BufferAttribute(positions,3));geometry.setIndex(indices);geometry.computeVertexNormals();return new THREE.Mesh(geometry,materials.sail)};export function createSails({materials,quality='medium'}){const group=new THREE.Group();group.name='VictorySails';[[-10.4,18,13.5,7],[-10.4,25,10.5,6],[-10.4,31,7,4.6],[1.3,19,16.5,8],[1.3,27,12.6,6.5],[1.3,34,8.4,4.8],[12.2,17,11.2,7],[12.2,24,8.2,5.5]].forEach(([z,y,width,height])=>{const sail=curvedSail(width,height,materials,quality);sail.position.set(0,y-.4,z);group.add(sail)});const mizzen=curvedSail(8.2,10.5,materials,quality);mizzen.position.set(0,23.8,14.2);mizzen.rotation.z=.22;group.add(mizzen);return group;}
